# Базовый образ
FROM python:alpine

# Дириктория внутри образа
WORKDIR /app

# Компирование всех файлов из текущей дириктории
COPY . .

# Интструкция того, какой процесс запускать при создании контейнера
CMD ["python", "main.py"]