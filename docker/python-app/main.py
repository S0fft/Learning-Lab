import calendar

print('Test example app for Docker learning')

year = int(input('Please enter the year: '))
month = int(input('Please enter the month: '))


print(calendar.month(year, month))
